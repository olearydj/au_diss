%%% Custom template for AU dissertation
%%% based on Quarto's standard template.tex

$if(has-frontmatter)$
  \frontmatter
$endif$

$if(title)$
  $if(beamer)$
    \frame{\titlepage}
  $else$
    \maketitle
  $endif$
  $if(abstract)$
    \begin{abstract}
      $abstract$
    \end{abstract}
  $endif$
$endif$
