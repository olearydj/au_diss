# Worktree Audit

This note records the main ignored/generated residue still present in the repo after the baseline checkpoint work, along with the current interpretation of what each item represents.

## Safe Local Noise

These are normal local-only artifacts and not part of the dissertation baseline:

- `.DS_Store`
- `.Rhistory`
- `.Rproj.user/`
- `.quarto/`
- `.venv/`
- `.vscode/`
- `diss.code-workspace`
- `parse_xml/__pycache__/`
- `renv/library/`

These should remain ignored.

## Direct Render Residue

These are generated by direct Quarto/LaTeX renders and are not source artifacts:

- `24-methods_files/`
- `_chaps/24-methods_files/`
- `_chaps/25-results_files/`
- `_chaps/25a-h1a.html`
- `_chaps/25a-h1a_files/`
- `_chaps/25b-h1b.html`
- `_chaps/25b-h1b_files/`
- `_chaps/25c-h1c.html`
- `_chaps/25c-h1c_files/`
- `_chaps/tinytable_assets/`
- `index_files/`
- `analysis/forms_data/forms_data.nb.html`
- root LaTeX aux files such as:
  - `Augmented-vs.-Traditional-Instruction-in-Manufacturing-Assembly.aux`
  - `Augmented-vs.-Traditional-Instruction-in-Manufacturing-Assembly.lof`
  - `Augmented-vs.-Traditional-Instruction-in-Manufacturing-Assembly.lot`
  - `Augmented-vs.-Traditional-Instruction-in-Manufacturing-Assembly.toc`
  - `Augmented-vs.-Traditional-Instruction-in-Manufacturing-Assembly.pdf`

Interpretation:

- these are normal render byproducts
- `24-methods_files/` in particular is just a direct chapter-render figure directory; locally it currently contains only `figure-pdf/fig-timeline-1.pdf`
- they should remain ignored and can be deleted locally whenever convenient

## Current Rebuild Output

- `manuscript/`

Interpretation:

- this is the current direct render output directory
- it is useful while baselining, but it is not the canonical defended artifact set
- the canonical defended-era PDFs live under [`defended-2024-08-02/`](/Volumes/Casa/dev/dissertation/defended-2024-08-02)

Current recommendation:

- keep `manuscript/` ignored as a working render target

## R Curation QA Outputs

- `analysis/forms_data/output/`

Interpretation:

- this directory is written by [`analysis/forms_data/forms_data.Rmd`](/Volumes/Casa/dev/dissertation/analysis/forms_data/forms_data.Rmd)
- it is not unit tests
- it contains QA/debug CSV dumps used to inspect intermediate curation results:
  - `combined.csv`
  - `joined.csv`
  - `joined_aug.csv`
  - `outcomes.csv`
  - `times.csv`
  - `tlx_scores.csv`

Current recommendation:

- keep ignored for now
- optionally archive elsewhere later if you decide these are worth preserving as pipeline evidence
- keep the notebook and its QA/debug outputs together under `analysis/forms_data/` so the curation step is structurally separate from manuscript source and curated data artifacts

## Python Parser Test Harness

- `parse_xml/test_input/`
- `parse_xml/test_output/`

Interpretation:

- these are parser/report-generation fixtures, not manuscript inputs
- `parse_xml/test_input/` contains sample XML files
- `parse_xml/test_output/` contains generated markdown reports from those samples

Current recommendation:

- normalize this into a fully tracked small fixture harness
- keep both the XML inputs and generated markdown outputs with [`parse_xml/`](/Volumes/Casa/dev/dissertation/parse_xml), since they are useful if the Python processing path is revived or refactored
- track the previously ignored local `1001-Learn.xml` as part of that normalization

## Data Dictionary Artifact

- `data/DataDictionary.docx`

Interpretation:

- auxiliary documentation artifact associated with the unfinished data-dictionary appendix stub
- not an active manuscript input
- historically meaningful enough to preserve

Current recommendation:

- track it explicitly in place for the baseline
- decide later whether to convert it into markdown or another repo-native format

## External Dependency Boundary

Some important inputs are not in the repo at all:

- ThunderBay participant/video/XML tree:
  - `/Volumes/ThunderBay mini/Research Master/data/`
- ThunderBay raw video archive:
  - `/Volumes/ThunderBay mini/Research Master/raw_videos/`
- Box Sync trial-data mirror:
  - `/Users/djo/Box Sync/Tiger Motors Research Team Collaboration Files/Investigation 1 Data Files/trial-data/`

These are required for full raw-data regeneration, but not for the current manuscript rebuild.

## Current Audit Summary

The remaining ignored worktree is now mostly understandable:

- local/editor noise
- direct render residue
- current working manuscript render output
- QA CSV dumps from `analysis/forms_data/forms_data.Rmd`
- parser test fixtures and outputs
- external machine-specific dependencies not preserved in the repo

What remains to decide is not what these things *are*, but whether any of the QA/test artifacts should be promoted from ignored local residue into documented archival support material.
