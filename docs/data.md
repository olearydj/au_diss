# Data Tree

This note records the current understanding of the dissertation data tree and how its major components relate to the analysis workflow.

## Overview

The repository preserves more than one layer of research data:

- source spreadsheets used for curation and manual correction
- generated participant notes/reports
- curated analysis workbooks
- saved R objects used directly by the manuscript build
- a separate retention/H3 support workbook added later

These layers should not be treated as duplicates. They represent different stages of the original workflow.

## Main Phase 1 / Phase 2 Data Path

The original study data flow for the main sessions appears to be:

1. Source workbook and manual corrections in `data/source/`
2. Additional processing and cleanup in `forms_data.Rmd` and the Python utilities in `parse_xml/`
3. Export of the curated workbook `data/combined_results.xlsx`
4. Loading of curated workbook tabs and saved objects during manuscript rendering

Key files in that path:

- `data/source/i1_raw_data.xlsx`
  - primary workbook source used in `forms_data.Rmd`
- `data/source/adjusted_drop_events.xlsx`
  - manual correction/support input used during event cleanup
- `data/combined_results.xlsx`
  - curated workbook exported from the original phase 1 / phase 2 processing path

The methods chapter and `forms_data.Rmd` both support this interpretation.

## Retention / H3 Data Path

Retention/H3 was handled differently.

- `forms_data.Rmd` exports `data/combined_results.xlsx` but still carries a TODO to "add phase 3"
- the retention analysis instead reads `data/source/i1_h3.xlsx` directly
- H3 then joins that sheet with:
  - `demographics` from `data/combined_results.xlsx`
  - recall-derived objects from `rdata/`

This split is now understood as intentional historical drift in the workflow:

- H3 analysis was deferred
- it was later completed more ad hoc
- the retention data was not folded back into the original aggregated workbook pipeline

So `data/source/i1_h3.xlsx` should be treated as a legitimate retained source artifact for the retention analysis, not as an orphaned duplicate.

## Qualitative Notes And Generated Reports

Two markdown trees preserve participant-level side records:

### `data/source/notes/`

- participant-level source notes
- primarily script notes and participant feedback
- appears to be manually maintained source material

### `data/reports/`

- generated participant reports
- include participant information, observational notes, and local video links
- appear to have been generated from the Python reporting utilities in `parse_xml/`

Relevant scripts:

- `parse_xml/process_data.py`
- `parse_xml/generate_combined_reports.py`
- `parse_xml/process_video_tree.py`

These report files are not used by the active Quarto manuscript build, but they are part of the preserved research record and help relate participant notes, workbook data, and video metadata to one another.

## Saved R Objects

`rdata/` contains saved objects used directly by the manuscript build, including:

- derived datasets such as `learn`, `recall`, and `res_f`
- shared helper functions/themes in `shared.RData`
- saved bootstrap outputs
- saved robust-model fits for H1b/H2 sections

These files are part of the current render path and should be treated as baseline-support artifacts rather than disposable cache.

## Current Baseline Interpretation

For current baselining purposes:

- keep the full `data/` tree
- treat `data/source/` as source/curation inputs
- treat `data/reports/` as generated but historically meaningful support artifacts
- treat `data/combined_results.xlsx` as the canonical curated workbook for the main study
- treat `data/source/i1_h3.xlsx` as the separate but legitimate H3 retention source workbook

## Future Documentation Work

The next useful refinement would be a fuller end-to-end provenance map showing:

- what was entered manually
- what was generated by Python
- what was cleaned/combined in R
- what was later added ad hoc for H3

That is a future task, but the distinctions above are enough to justify retaining the current full data tree in the repository baseline.
