\documentclass[
    fontsize=11,
    paper=letter,
    usegeometry=true  % improve compat with geo pkg
  ]{scrreprt}

\usepackage[
    showframe=true,
    margin=1in,
    bindingoffset=0.5in,
    heightrounded=true,
    footskip=0.475in
  ]{geometry}


\usepackage{lipsum}

% control line spacing within paragraphs
\usepackage{setspace}
\singlespacing  % set default single space

% control spacing b/w paragraphs and indention
%\usepackage{parskip}

% redefine length of paragraph indent
%\setlength{\parindent}{2in}

% redefine indent command to override default
% parindent of zero with parskip
\renewcommand{\indent}{\hspace{0.5in}}

\addtokomafont{disposition}{\setstretch{1}}
\RedeclareSectionCommand[beforeskip=0pt, afterskip=2\baselineskip]{chapter}
%\setkomafont{chapter}{\large\sffamily}
\renewcommand\raggedchapter{\centering}

\KOMAoptions{parskip=half+}
\setstretch{1.325}
\newlength{\blankline}
\setlength{\blankline}{\the\baselineskip}
\singlespacing
\KOMAoptions{parskip=false}

\begin{document}
The value of \verb|\blankline| is \texttt{\the\blankline}.

The following text should be indented with single spaced lines and no space between paragraphs.

\lipsum[1-2]

\tableofcontents

\chapter{First Chapter}
\begin{spacing}{1.325}
\KOMAoptions{parskip=half+}
\indent
This if the first paragraph.
\lipsum[][1-3]

And this is the second.
\lipsum[][4-6]
\end{spacing}

\chapter{Second Chapter}
Last paragraph.

\lipsum[][7-9]
\end{document}

\documentclass[]{report}
\usepackage[showframe,pass]{geometry}
